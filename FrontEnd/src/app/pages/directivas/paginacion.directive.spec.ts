import { PaginacionDirective } from './paginacion.directive';

describe('PaginacionDirective', () => {
  it('should create an instance', () => {
    const directive = new PaginacionDirective();
    expect(directive).toBeTruthy();
  });
});
